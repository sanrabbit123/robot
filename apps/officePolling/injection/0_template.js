/*

Result Folder :
  inClass => this.dir (last \ exist)
  Local => __defaultResultFolder__ (last \ no)

ExecMain.prototype.launching = async function () {
  try {

  } catch (e) {
    alert(e);
  }
}

const aiInstance = new ExecMain({}, __defaultResultFolder__, {});
aiInstance.launching();
app.quit();

*/
