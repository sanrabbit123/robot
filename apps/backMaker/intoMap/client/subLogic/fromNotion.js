module.exports = function (tools) {
  const { Mother, Notion, Filters } = tools;
  return async function (tong) {
    try {
      // let totalTong = [];

      // return totalTong;

      return tong;

    } catch (e) {
      console.log(e);
    }
  }
}
